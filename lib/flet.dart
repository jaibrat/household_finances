import 'package:flutter/material.dart';

class FletScreen extends StatelessWidget {
  const FletScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Flet File Search")),
      body: const Center(child: Text("To be continued...")),
    );
  }
}